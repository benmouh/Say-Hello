language: c++

os : 
 Linux

script:
 - npm install -g node-gyp
 - node-gyp configure
 - node-gyp build
 - npm test
 - cppcheck  main.cc -v -xml
install:
 - npm install -g mocha
 - sudo apt-get install cppcheck
deploy:
  provider: npm
  email: aitbenmouh@gmail.com
  api_key: $apikey