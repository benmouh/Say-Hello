language: c++

os : 
 Linux

script:
 - npm install -g node-gyp
 - node-gyp configure
 - node-gyp build
 - npm test
 - cppcheck  --language=c++ --std=c++11 --quiet --error-exitcode=1 . 
install:
 - npm install -g mocha
 - sudo apt-get install -qq cppcheck
deploy:
  provider: npm
  email: aitbenmouh@gmail.com
  api_key: $apikey